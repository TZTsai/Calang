compose(f, g) := function x: f(g(x))
identity := function x: x
fold(f, times) := identity if times=0 else compose(f, fold(f, times-1)) \
if times%2 else fold(compose(f, f), times/2);

pow(x, n) := fold(function y: x*y, n)(1)
pow(2, 5) #32